% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{resolve_txt_selected_wells}
\alias{resolve_txt_selected_wells}
\title{Resolve the set of well keys to display from selection inputs}
\usage{
resolve_txt_selected_wells(
  df,
  plate_ids = NULL,
  selected_conditions = character(0),
  selected_wells = character(0)
)
}
\arguments{
\item{df}{TXT data frame (output of \code{\link[=build_txt_trajectory_df]{build_txt_trajectory_df()}} or
similar). Must contain \code{plate_id}, \code{animal}, and \code{condition_grouped}.}

\item{plate_ids}{Character vector of plate IDs to include. \code{NULL} =
all plates.}

\item{selected_conditions}{Character vector of \code{condition_grouped}
values to include.}

\item{selected_wells}{Character vector of specific \code{well_key} values to
include (intersected with available wells). If empty and
\code{selected_conditions} is non-empty, all wells matching the conditions
are returned.}
}
\value{
Character vector of \code{well_key} values.
}
\description{
Filters a TXT data frame by plate IDs and conditions, then returns
the matching set of \code{well_key} identifiers (\code{"<plate_id>__<animal>"}).
}
\keyword{internal}
