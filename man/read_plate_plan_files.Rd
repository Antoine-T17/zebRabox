% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plate_plan.R
\name{read_plate_plan_files}
\alias{read_plate_plan_files}
\title{Read and sort plate plan Excel files}
\usage{
read_plate_plan_files(paths, names = basename(paths))
}
\arguments{
\item{paths}{Character vector of file paths.}

\item{names}{Character vector of display names the same length as
\code{paths}. Defaults to \code{basename(paths)}, so it can be omitted in
standalone use. In a Shiny session pass \code{input$files$name} explicitly.}
}
\value{
A list of data frames, one per file, in alphabetical order of
\code{names}.
}
\description{
Reads one or more \code{.xlsx} plate plan files in a single pass, adds a
\code{plate_id} column when absent, attaches the original filename as the
\code{file_name} attribute, and returns the data frames sorted alphabetically
(case-insensitive) by display name — matching the convention used for
raw XLSX files throughout the app.
}
\seealso{
\code{\link[=create_plate_plan]{create_plate_plan()}}, \code{\link[=run_processing]{run_processing()}}
}
