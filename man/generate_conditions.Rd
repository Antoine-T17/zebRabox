% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processing.R
\name{generate_conditions}
\alias{generate_conditions}
\title{Assign condition columns from a plate plan to raw data}
\usage{
generate_conditions(
  current_data,
  current_plan,
  i,
  log_fn = function(msg) invisible(NULL)
)
}
\arguments{
\item{current_data}{Data frame of raw data for one plate. Must contain
an \code{animal} column.}

\item{current_plan}{Data frame of the plate plan. Must contain
\code{animal}, \code{condition}, and \code{plate_id} columns.}

\item{i}{Integer plate index, used in log and error messages.}

\item{log_fn}{Function accepting a single character string; called for
diagnostic messages. Default: silent.}
}
\value{
A named list with two elements:
\describe{
\item{data}{Updated raw data frame with condition columns added.}
\item{plan}{Updated plate plan with \code{condition_grouped} and
\code{condition_tagged} columns added (if they were absent).}
}
}
\description{
Joins the plate plan to the raw data frame on the animal identifier
(stripping any \verb{_plate_N} suffix) and adds three columns:
\code{condition}, \code{condition_grouped} (base label without replicate suffix),
and \code{condition_tagged} (base label with a per-group row index).
}
\keyword{internal}
