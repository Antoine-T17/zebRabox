% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{read_raw_files}
\alias{read_raw_files}
\title{Read multiple raw ZebraBox data files}
\usage{
read_raw_files(paths, names = basename(paths))
}
\arguments{
\item{paths}{Character vector of file paths.}

\item{names}{Character vector of display names the same length as \code{paths}.
Defaults to \code{basename(paths)}. Typically omitted in standalone use.}
}
\value{
A named list of data frames in alphabetical order of \code{names}. Each
data frame carries a \code{file_name} attribute set to the corresponding
display name.
}
\description{
Convenience wrapper around \code{\link[=read_raw_file]{read_raw_file()}} for batch loading. Reads all
files in a single call, sorts them alphabetically by display name
(case-insensitive) — matching the convention used for plate plans — and
returns a list ready to pass to \code{\link[=run_processing]{run_processing()}} as \code{raw_xlsx_list}.
}
\examples{
\dontrun{
raw_data <- read_raw_files(c("plate1.xlsx", "plate2.xlsx"))
result   <- run_processing(raw_xlsx_list = raw_data, ...)
}
}
\seealso{
\code{\link[=read_raw_file]{read_raw_file()}}, \code{\link[=run_processing]{run_processing()}}
}
