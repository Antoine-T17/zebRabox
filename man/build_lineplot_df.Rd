% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{build_lineplot_df}
\alias{build_lineplot_df}
\title{Build lineplot dataset with per-well temporal aggregation}
\usage{
build_lineplot_df(az, v, agg_period, unit_from, unit_to, convert)
}
\arguments{
\item{az}{Data frame from \code{\link[=prepare_all_zone]{prepare_all_zone()}}.}

\item{v}{Name of the response variable column.}

\item{agg_period}{Aggregation period (numeric, in \code{unit_from} or
\code{unit_to} depending on \code{convert}).}

\item{unit_from}{Original time unit of the \code{start} column.}

\item{unit_to}{Target time unit (used when \code{convert == "Yes"}).}

\item{convert}{\code{"Yes"} or \code{"No"} — whether to convert \code{start} to
\code{unit_to} before binning.}
}
\value{
Data frame with columns \code{zone}, \code{condition_grouped},
\code{start_rounded}, \code{plate_id}, \code{animal}, \code{total_val}, \code{n_wells},
\code{val_per_well}, \code{se_per_well}, \code{sd_per_well}.
}
\description{
Sums the response variable within each time bin per well, then
computes cross-well mean, SD, SE, and n per condition group × zone.
}
\seealso{
\code{\link[=plot_lineplot]{plot_lineplot()}}, \code{\link[=prepare_all_zone]{prepare_all_zone()}}, \code{\link[=prepare_dataset]{prepare_dataset()}}
}
