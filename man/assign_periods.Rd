% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processing.R
\name{assign_periods}
\alias{assign_periods}
\title{Assign period labels to raw data rows based on time boundaries}
\usage{
assign_periods(
  current_data,
  period_df,
  i,
  cfg,
  log_fn = function(msg) invisible(NULL)
)
}
\arguments{
\item{current_data}{Data frame with a numeric \code{start} column
(seconds from experiment start).}

\item{period_df}{Data frame with columns \code{start} (numeric) and
\code{transition} (character, e.g. \code{"light1-dark1"}), sorted ascending.}

\item{i}{Integer plate index.}

\item{cfg}{Processing config list; must contain a \code{period_map}
function if period-number stripping is desired (see
\code{\link[=get_processing_config]{get_processing_config()}}).}

\item{log_fn}{Logging function.}
}
\value{
A named list:
\describe{
\item{data}{Updated data frame with \code{period_with_numbers} and
\code{period_without_numbers} columns.}
\item{boundaries}{Numeric vector of transition start times.}
\item{transitions}{Character vector of transition labels.}
}
}
\description{
Uses the period-transition file to label each row with
\code{period_with_numbers} (e.g. \code{"light1"}, \code{"dark1"}) and
\code{period_without_numbers} (e.g. \code{"light"}, \code{"dark"}) via
\code{cfg$period_map}.
}
\keyword{internal}
