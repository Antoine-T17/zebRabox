% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{export_results}
\alias{export_results}
\title{Export processed results to disk}
\usage{
export_results(result, path = ".", format = "xlsx")
}
\arguments{
\item{result}{Named list returned by \code{\link[=run_processing]{run_processing()}}. Must contain
a \code{processed_data_list} slot.}

\item{path}{Output directory (created automatically if it does not exist).
Default \code{"."} (current working directory).}

\item{format}{Output format: \code{"xlsx"} (default) or \code{"csv"}.}
}
\value{
Invisibly, the path(s) of the file(s) written.
}
\description{
Writes the \code{processed_data_list} slot of a \code{\link[=run_processing]{run_processing()}} result
object to one or more files. In xlsx format all plates are written into
a single workbook (\code{processed_results.xlsx}), one sheet per plate.
In csv format each plate is written to a separate file
(\verb{plate_<id>.csv}).
}
\examples{
\dontrun{
result <- run_processing(raw_xlsx_list, plate_plans, period_df,
                         removal_df, cfg$processing)
export_results(result, path = "output/")
export_results(result, path = "output/", format = "csv")
}
}
\seealso{
\code{\link[=run_processing]{run_processing()}}, \code{\link[=export_figures]{export_figures()}}
}
