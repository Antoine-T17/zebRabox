% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{export_figures}
\alias{export_figures}
\title{Export a list of ggplot figures to image files}
\usage{
export_figures(
  figure_list,
  path = ".",
  format = "png",
  width = 10,
  height = 7,
  dpi = 300,
  ...
)
}
\arguments{
\item{figure_list}{Named list of \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} objects. Names are
used as file base names (e.g. \code{list(periods = p1, cumulative = p2)}
produces \code{periods.png} and \code{cumulative.png}). Unnamed lists fall back to
\code{figure_1}, \code{figure_2}, etc.}

\item{path}{Output directory (created automatically if it does not exist).
Default \code{"."} (current working directory).}

\item{format}{Image format passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}: \code{"png"}
(default), \code{"pdf"}, \code{"svg"}, \code{"jpg"}, or \code{"tiff"}.}

\item{width}{Figure width in inches (default 10).}

\item{height}{Figure height in inches (default 7).}

\item{dpi}{Resolution in dots per inch (default 300, ignored for vector
formats).}

\item{...}{Additional arguments forwarded to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
Invisibly, a named character vector of file paths written.
}
\description{
Saves each element of a named list of \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} objects to
\verb{<path>/<name>.<format>} using \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.
}
\examples{
\dontrun{
cfg     <- set_mode("tracking", "light_dark")
dataset <- prepare_dataset(result, cfg$visualization)
figs    <- list(
  periods    = plot_periods(dataset, "inact", cfg$visualization),
  cumulative = plot_cumulative(dataset, "inact")
)
export_figures(figs, path = "output/figures/")
}
}
\seealso{
\code{\link[=plot_periods]{plot_periods()}}, \code{\link[=plot_cumulative]{plot_cumulative()}}, \code{\link[=plot_lineplot]{plot_lineplot()}},
\code{\link[=plot_delta]{plot_delta()}}, \code{\link[=export_results]{export_results()}}
}
