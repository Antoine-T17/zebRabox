% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{create_period_table}
\alias{create_period_table}
\title{Create a period-transitions table}
\usage{
create_period_table(start, transition)
}
\arguments{
\item{start}{Numeric vector of transition start times in seconds
(e.g. \code{c(0, 600, 1200)}). The first value is typically \code{0}.}

\item{transition}{Character vector of transition labels the same length as
\code{start}. Each label must follow the \code{"periodA-periodB"} convention
(e.g. \code{"light1-dark1"}). The left part is the period active
\emph{before} the transition, the right part is active \emph{after}.}
}
\value{
A data frame with columns \code{start} (numeric) and \code{transition}
(character), ready to be passed to \code{\link[=run_processing]{run_processing()}} as \code{period_df}.
}
\description{
Builds the \code{period_df} data frame required by \code{\link[=run_processing]{run_processing()}} from
plain R vectors, without needing to prepare an Excel file.
}
\examples{
period_df <- create_period_table(
  start      = c(0, 600, 1200),
  transition = c("light1-dark1", "dark1-light2")
)
}
\seealso{
\code{\link[=run_processing]{run_processing()}}, \code{\link[=create_removal_table]{create_removal_table()}}
}
