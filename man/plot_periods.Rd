% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_periods}
\alias{plot_periods}
\title{Plot per-period boxplots for a response variable}
\usage{
plot_periods(
  dataset,
  variable,
  cfg,
  zone = NULL,
  mode = "separated",
  period_colors = NULL,
  period_indices_keep = NULL,
  condition_order = NULL,
  condition_colors = NULL,
  theme = "light"
)
}
\arguments{
\item{dataset}{Data frame from \code{\link[=prepare_dataset]{prepare_dataset()}} or \code{\link[=prepare_all_zone]{prepare_all_zone()}}.}

\item{variable}{Name of the response variable column.}

\item{cfg}{Visualization config list from \code{\link[=get_visualization_config]{get_visualization_config()}} or
\code{\link[=set_mode]{set_mode()}}\verb{$visualization}.}

\item{zone}{Zone identifier (e.g. \code{"0"}, \code{"2"}). If \code{NULL} the first
available zone is used.}

\item{mode}{Boxplot layout: \code{"separated"} (one facet per period, default) or
\code{"pooled"} (conditions on x-axis, periods as fill).}

\item{period_colors}{Character vector of hex colors for periods in pooled
mode. Recycled or truncated as needed.}

\item{period_indices_keep}{Optional comma-separated string of period indices
to retain (e.g. \code{"1"} or \code{"1,2"}). \code{NULL} keeps all.}

\item{condition_order}{Character vector specifying display order of
conditions. Unknown values are ignored; missing conditions are appended.}

\item{condition_colors}{Character vector of hex colors for conditions.}

\item{theme}{\code{"light"} (default) or \code{"dark"}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Combines dataset preparation (\code{\link[=build_periods_df]{build_periods_df()}}) and plotting into a
single call. Returns a \code{ggplot} object ready for display or export.
}
\examples{
\dontrun{
cfg     <- set_mode("tracking", "light_dark")
result  <- run_processing(raw_xlsx_list, plate_plans, period_df,
                          removal_df, cfg$processing)
dataset <- prepare_dataset(result, cfg$visualization)
p <- plot_periods(dataset, variable = "inact", cfg = cfg$visualization)
}
}
\seealso{
\code{\link[=build_periods_df]{build_periods_df()}}, \code{\link[=prepare_dataset]{prepare_dataset()}}, \code{\link[=set_mode]{set_mode()}}
}
