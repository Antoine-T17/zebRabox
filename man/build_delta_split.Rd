% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{build_delta_split}
\alias{build_delta_split}
\title{Build delta-phase dataset around a transition time point}
\usage{
build_delta_split(
  az,
  vars,
  transition,
  delta_sec,
  boundaries_df,
  round_to = NULL
)
}
\arguments{
\item{az}{Data frame from \code{\link[=prepare_all_zone]{prepare_all_zone()}}.}

\item{vars}{Character vector of response variable names.}

\item{transition}{Character string identifying the transition (must
match values in \code{boundaries_df$transition}).}

\item{delta_sec}{Length of each phase window in seconds.}

\item{boundaries_df}{Data frame with columns \code{plate_id}, \code{time_switch},
and \code{transition} (typically
\code{dplyr::bind_rows(rv$processing_results$boundary_associations_list) |> dplyr::distinct()}).}

\item{round_to}{Optional numeric; if provided, \code{start} values are
rounded down to the nearest multiple before phase assignment.}
}
\value{
Named list of data frames (one per variable in \code{vars}), or
\code{NULL} if no data matches the transition.
}
\description{
Assigns each row to one of three phases — Before \verb{[t-Δ, t)},
Switch \verb{[t, t+Δ)}, After \verb{[t+Δ, t+2Δ)} — relative to the transition
time for each plate, then summarises the mean of each response
variable per phase × animal. Returns a named list (one element per
variable in \code{vars}).
}
\seealso{
\code{\link[=plot_delta]{plot_delta()}}, \code{\link[=get_boundaries]{get_boundaries()}}, \code{\link[=prepare_dataset]{prepare_dataset()}}
}
