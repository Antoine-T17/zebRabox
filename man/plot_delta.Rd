% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_delta}
\alias{plot_delta}
\title{Plot delta-phase boxplots around a transition time point}
\usage{
plot_delta(
  dataset,
  variable,
  transition,
  delta_sec = 60,
  boundaries_df,
  zone = NULL,
  mode = "separated",
  phase_colors = NULL,
  condition_order = NULL,
  condition_colors = NULL,
  theme = "light"
)
}
\arguments{
\item{dataset}{Data frame from \code{\link[=prepare_dataset]{prepare_dataset()}} or \code{\link[=prepare_all_zone]{prepare_all_zone()}}.}

\item{variable}{Name of the response variable column.}

\item{transition}{Transition label matching a value in
\code{boundaries_df$transition} (e.g. \code{"light1-dark1"}).}

\item{delta_sec}{Length of each phase window in seconds (default 60).}

\item{boundaries_df}{Data frame with columns \code{plate_id}, \code{time_switch}, and
\code{transition} (output of \code{\link[=get_boundaries]{get_boundaries()}}).}

\item{zone}{Zone identifier. If \code{NULL} the first available zone is used.}

\item{mode}{Boxplot layout: \code{"separated"} (one facet per phase, default) or
\code{"pooled"} (conditions on x-axis, phases as fill).}

\item{phase_colors}{Character vector of hex colors for phases in pooled mode
(Before, Switch, After order). Recycled or truncated as needed.}

\item{condition_order}{Character vector specifying display order of conditions.}

\item{condition_colors}{Character vector of hex colors for conditions.}

\item{theme}{\code{"light"} (default) or \code{"dark"}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Splits data into Before / Switch / After windows (\code{\link[=build_delta_split]{build_delta_split()}})
and plots per-condition boxplots. Returns an empty plot with a message
when no data fall within the delta window.
}
\examples{
\dontrun{
bounds <- get_boundaries(result)
p <- plot_delta(dataset, variable = "inact", transition = "light1-dark1",
                delta_sec = 120, boundaries_df = bounds)
}
}
\seealso{
\code{\link[=build_delta_split]{build_delta_split()}}, \code{\link[=get_boundaries]{get_boundaries()}}, \code{\link[=prepare_dataset]{prepare_dataset()}}
}
