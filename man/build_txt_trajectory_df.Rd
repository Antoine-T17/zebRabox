% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{build_txt_trajectory_df}
\alias{build_txt_trajectory_df}
\title{Build a binned XY trajectory dataset for TXT visualization}
\usage{
build_txt_trajectory_df(df, well_keys, time_range, target_points = 100)
}
\arguments{
\item{df}{TXT data frame with columns \code{T}, \code{X}, \code{Y}, \code{plate_id},
\code{animal}, \code{condition_grouped}, \code{condition_tagged}, and \code{zone}.}

\item{well_keys}{Character vector of \code{well_key} values.}

\item{time_range}{Numeric vector \code{c(tmin, tmax)} in seconds.}

\item{target_points}{Maximum number of displayed points per well
after binning (default 100).}
}
\value{
Data frame with binned trajectory points. Attributes
\code{bin_s}, \code{native_dt}, and \code{time_range} are attached.
}
\description{
Filters the TXT data to the selected wells and time window, removes
\verb{(0,0)} origin points, bins the remaining rows into temporal bins
sized to keep at most \code{target_points} per well, and averages X/Y
within each bin.
}
\keyword{internal}
