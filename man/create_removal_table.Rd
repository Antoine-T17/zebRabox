% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{create_removal_table}
\alias{create_removal_table}
\title{Create a removal-specifications table}
\usage{
create_removal_table(
  plate_id,
  remove_time_codes = NA_character_,
  remove_wells = NA_character_,
  remove_conditions = NA_character_,
  remove_periods = NA_character_
)
}
\arguments{
\item{plate_id}{Integer or character vector of plate identifiers.
Numeric digits are extracted automatically, so \code{"Plate1"}, \code{"plate_1"},
and \code{1L} all map to plate \code{1}.}

\item{remove_time_codes}{Comma-separated numeric time codes (seconds) to
remove. Default \code{NA} (no removal).}

\item{remove_wells}{Comma-separated well names to remove
(e.g. \code{"A01,B02"}). Default \code{NA}.}

\item{remove_conditions}{Comma-separated base condition names to remove
(e.g. \code{"ctrl"}). Default \code{NA}.}

\item{remove_periods}{Comma-separated period names (with numbers) to
remove (e.g. \code{"light1"}). Default \code{NA}.}
}
\value{
A data frame with columns \code{plate_id} (numeric), \code{remove_time_codes},
\code{remove_wells}, \code{remove_conditions}, and \code{remove_periods} (all character),
ready to be passed to \code{\link[=run_processing]{run_processing()}} as \code{removal_df}.
}
\description{
Builds the \code{removal_df} data frame required by \code{\link[=run_processing]{run_processing()}} from
plain R vectors, without needing to prepare an Excel file.
}
\details{
Each argument except \code{plate_id} can be:
\itemize{
\item \code{NA} (default) — skip this removal step for the plate(s).
\item A single string applied to all plates (e.g. \code{"A01,B02"}).
\item A character vector of length equal to \code{plate_id}, one spec per
plate.
}
Multiple items within one spec are comma-separated
(e.g. \code{remove_wells = "A01,B02"}).
}
\examples{
# No removals (all NAs)
removal_df <- create_removal_table(plate_id = c(1, 2))

# Remove specific wells on plate 1 only
removal_df <- create_removal_table(
  plate_id     = c(1, 2),
  remove_wells = c("A01,B02", NA)
)
}
\seealso{
\code{\link[=run_processing]{run_processing()}}, \code{\link[=create_period_table]{create_period_table()}}
}
