% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{prepare_dataset}
\alias{prepare_dataset}
\title{Prepare a combined dataset for visualization}
\usage{
prepare_dataset(result, cfg)
}
\arguments{
\item{result}{Named list returned by \code{\link[=run_processing]{run_processing()}}. Must contain
a \code{processed_data_list} slot.}

\item{cfg}{Visualization config list, either from
\code{\link[=get_visualization_config]{get_visualization_config()}} or from \code{\link[=set_mode]{set_mode()}}\verb{$visualization}.}
}
\value{
A single data frame with all plates row-bound and TM-derived
metrics computed (same as \code{\link[=prepare_all_zone]{prepare_all_zone()}}).
}
\description{
User-friendly wrapper around \code{\link[=prepare_all_zone]{prepare_all_zone()}}. Accepts the list
returned by \code{\link[=run_processing]{run_processing()}} directly, so the caller does not need
to extract \code{result$processed_data_list} manually.
}
\examples{
\dontrun{
cfg     <- set_mode("tracking", "light_dark")
result  <- run_processing(raw_xlsx_list, plate_plans, period_df,
                          removal_df, cfg$processing)
dataset <- prepare_dataset(result, cfg$visualization)
}
}
\seealso{
\code{\link[=prepare_all_zone]{prepare_all_zone()}}, \code{\link[=run_processing]{run_processing()}}, \code{\link[=set_mode]{set_mode()}}
}
