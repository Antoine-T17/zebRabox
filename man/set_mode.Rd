% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{set_mode}
\alias{set_mode}
\title{Select an analytical mode and retrieve both configuration objects}
\usage{
set_mode(primary, secondary)
}
\arguments{
\item{primary}{Character. Acquisition type: \code{"tracking"} (Tracking Mode)
or \code{"quantization"} (Quantization Mode). Case-insensitive.}

\item{secondary}{Character. Stimulus type: \code{"light_dark"} (Light/Dark
alternation) or \code{"vibration"} (Vibration/Rest alternation).
Case-insensitive.}
}
\value{
A named list with three elements:
\describe{
\item{\code{processing}}{Config list for \code{\link[=run_processing]{run_processing()}}, identical to
the output of \code{\link[=get_processing_config]{get_processing_config()}}.}
\item{\code{visualization}}{Config list for visualization helpers, identical
to the output of \code{\link[=get_visualization_config]{get_visualization_config()}}.}
\item{\code{mode_key}}{Character. The internal mode key derived from the
inputs (e.g. \code{"tm_ldm"}), useful for troubleshooting.}
}
}
\description{
User-friendly wrapper around \code{\link[=get_processing_config]{get_processing_config()}} and
\code{\link[=get_visualization_config]{get_visualization_config()}}. Accepts plain English terms instead of the
internal mode key (\code{"tm_ldm"}, etc.) and returns both configs in a single
list, ready to be passed to \code{\link[=run_processing]{run_processing()}} and the visualization
helpers.
}
\examples{
cfg <- set_mode("tracking", "light_dark")
result  <- run_processing(..., cfg = cfg$processing)
dataset <- prepare_all_zone(result$processed_data_list, cfg$visualization)
}
\seealso{
\code{\link[=get_processing_config]{get_processing_config()}}, \code{\link[=get_visualization_config]{get_visualization_config()}},
\code{\link[=run_processing]{run_processing()}}
}
