% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processing.R
\name{process_zones}
\alias{process_zones}
\title{Split data by zone and compute zone 1 as zone 0 minus zone 2}
\usage{
process_zones(current_data, i, cfg, log_fn = function(msg) invisible(NULL))
}
\arguments{
\item{current_data}{Data frame with an \code{an} column identifying zones.}

\item{i}{Integer plate index.}

\item{cfg}{Processing config list; must contain \code{zone_num_cols}
(character vector of numeric column names to subtract).}

\item{log_fn}{Logging function.}
}
\value{
Data frame with all zones row-bound, including a \code{zone}
column and only the columns listed in the standard output set.
}
\description{
Splits the data frame by the \code{an} column (zone identifier). If zones
0 and 2 are both present, creates zone 1 by key-joining zones 0 and 2
and subtracting the numeric metric columns listed in
\code{cfg$zone_num_cols}. Returns a single data frame with a \code{zone} column.
}
\keyword{internal}
